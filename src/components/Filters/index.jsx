/** @format */

import React from "react";
import SearchBox from "./SearchBox";

const Filters = () => {
  return (
    <React.Fragment>
      <SearchBox />
    </React.Fragment>
  );
};

export default Filters;
