/** @format */

import React from "react";
import { footerText } from "../config";

const Footer = () => {
  return (
    <footer>
      <p>{footerText}</p>
    </footer>
  );
};

export default Footer;
