/** @format */

import React from "react";
import { footerText } from "../config";

const Footer = () => {
  return (
    <React.Fragment>
      <footer>
        <p>{footerText}</p>
      </footer>
    </React.Fragment>
  );
};

export default Footer;
